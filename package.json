{
  "type": "module",
  "scripts": {
    "test": "npm run test:data-permissions",
    "test:data-permissions": "vitest run --config vitest.config.ts",
    "typecheck:test": "tsc -p tsconfig.test.json"
  },
  "dependencies": {
    "@instantdb/react": "^1.0.65"
  },
  "devDependencies": {
    "@instantdb/admin": "^1.0.65",
    "@instantdb/platform": "^1.0.65",
    "@types/node": "^26.2.0",
    "effect": "^3.22.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
