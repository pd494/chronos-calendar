# Full Review

Run `/security-review`, `/code-review`, and `code-simplifier` the three plugins installed in parallel subagents $ARGUMENTS (or uncomittted changes if not specified).

Output a consolidated report with findings from all three.
